import { ViewConfig } from "@vaadin/hilla-file-router/types.js";

export const config : ViewConfig = {
    title: "Teams"
}

export default function NoTeamSelectedView() {
    return "No team selected";
}